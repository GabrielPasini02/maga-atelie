# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/

# Claude Code (sessões locais, não devem ir pro repo)
.claude/projects/
.claude/sessions/
.claude/shell-snapshots/
.claude/telemetry/
.claude/file-history/
.claude/backups/

# Dependências
node_modules/

# Arquivos temporários
*.log
*.tmp

# Drop zone do usuário (só o README vai versionado)
dados/*
!dados/README.md

site/imagens/_backup-preotim/

# Notas internas e privadas — NUNCA vão pro repo público
_memoria/

# Contratos de clientes (dados pessoais) — só no PC
Contrato*.docx
Contrato*.pdf
Contrato*.html
Contrato*.md

# Estrutura interna / scaffolding — fica só no PC, fora do repo público
# (o repo remoto já removeu tudo isso de propósito)
CLAUDE.md
identidade/
marketing/
saidas/
scripts/
templates/
